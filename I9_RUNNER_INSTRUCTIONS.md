# 🤖 Gemini CLI: Intel i9 Runner Operational Instructions

## 📍 Current Status
You are the primary worker for the **starisdons** project. The Termux/Android runner is currently experiencing DNS loop issues and is effectively offline.

## 🛠️ New CI Architecture (v2.0)
The pipeline has been heavily optimized for your i9 hardware while respecting Windows filesystem constraints.

### 1. Sequential Execution Protocol
- **Constraint**: The runner environment does not support `custom_build_dir`.
- **Solution**: All Windows jobs are now gated by `resource_group: windows_i9_runner`.
- **Action**: Do not attempt to parallelize Windows jobs via the `.gitlab-ci.yml` without ensuring unique directory paths, otherwise, Git will fail with `index.lock` errors.

### 2. Smart Runner Scripts
- Use `ci/smart-runner-windows.ps1` for all tasks.
- It contains **Strict Architecture Gating**. If you see an architecture mismatch error, it means a job was incorrectly routed. Let it fail fast.
- It uses `robocopy /MT:32` for high-speed asset synchronization.

### 3. Performance Flags
- `FF_USE_FASTZIP` is enabled.
- `ARTIFACT_COMPRESSION_LEVEL` is set to `fastest`.
- Coverage collection is restricted to core files via `jest.config.cjs` to prevent log bloat.

## ⚠️ Known Issues
- **Termux DNS**: The Android runner is failing with `dial tcp: lookup gitlab.com on [::1]:53: connection refused`. This is a Go-specific resolver issue in Termux. Ignore this on the Windows side.
- **Git Locks**: If you encounter `index.lock` or `shallow.lock` errors, ensure the `resource_group` is active in the CI config.

## 🚀 Optimization Goal
Your goal is to maintain maximum throughput for `test:windows`, `build:windows`, and `deploy_mechgen_web:windows` while ensuring the workspace remains clean.

---
*Instructions generated by Gemini CLI (Termux Instance) on Tuesday, Jan 6, 2026.*
